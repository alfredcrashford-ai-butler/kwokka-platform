import { AccessRightMiddleware } from './access-right.middleware';

describe(AccessRightMiddleware, () => {
  it('exists', () => {
    expect(AccessRightMiddleware).toBeTruthy();
  });

  // TODO: add tests https://mygameapp.atlassian.net/browse/KWOKKA-395
});
