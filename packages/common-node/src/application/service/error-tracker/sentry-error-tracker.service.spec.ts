import { SentryErrorTrackerService } from './sentry-error-tracker.service';

describe(SentryErrorTrackerService, () => {
  it('exists', () => {
    expect(SentryErrorTrackerService).toBeTruthy();
  });

  // TODO: add tests https://mygameapp.atlassian.net/browse/KWOKKA-395
});
