import { WinstonLoggerService } from './winston-logger.service';

describe(WinstonLoggerService, () => {
  it('exists', () => {
    expect(WinstonLoggerService).toBeTruthy();
  });

  // TODO: add tests https://mygameapp.atlassian.net/browse/KWOKKA-395
});
