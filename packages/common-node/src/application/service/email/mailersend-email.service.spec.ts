import { MailersendEmailService } from './mailersend-email.service';

describe(MailersendEmailService, () => {
  it('exists', () => {
    expect(MailersendEmailService).toBeTruthy();
  });

  // TODO: add tests https://mygameapp.atlassian.net/browse/KWOKKA-395
});
