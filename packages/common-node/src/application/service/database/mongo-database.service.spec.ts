import { MongoDatabaseService } from './mongo-database.service';

describe(MongoDatabaseService, () => {
  it('exists', () => {
    expect(MongoDatabaseService).toBeTruthy();
  });

  // TODO: add tests https://mygameapp.atlassian.net/browse/KWOKKA-395
});
