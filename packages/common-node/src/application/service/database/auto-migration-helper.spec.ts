import { AutoMigrationHelper } from './auto-migration-helper';

describe(AutoMigrationHelper, () => {
  it('exists', () => {
    expect(AutoMigrationHelper).toBeTruthy();
  });

  // TODO: add tests https://mygameapp.atlassian.net/browse/KWOKKA-395
});
