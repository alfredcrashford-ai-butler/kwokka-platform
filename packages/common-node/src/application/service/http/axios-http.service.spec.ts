import { AxiosHttpService } from './axios-http.service';

describe(AxiosHttpService, () => {
  it('exists', () => {
    expect(AxiosHttpService).toBeTruthy();
  });

  // TODO: add tests https://mygameapp.atlassian.net/browse/KWOKKA-395
});
