import { MessageConsumer } from './message-consumer';

describe(MessageConsumer, () => {
  it('exists', () => {
    expect(MessageConsumer).toBeTruthy();
  });

  // TODO: add tests https://mygameapp.atlassian.net/browse/KWOKKA-395
});
