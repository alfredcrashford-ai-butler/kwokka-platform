import { KafkaMessageService } from './kafka-message.service';

describe(KafkaMessageService, () => {
  it('exists', () => {
    expect(KafkaMessageService).toBeTruthy();
  });

  // TODO: add tests https://mygameapp.atlassian.net/browse/KWOKKA-395
});
