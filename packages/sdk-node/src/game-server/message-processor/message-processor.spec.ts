import { MessageProcessor } from './message-processor';

describe(MessageProcessor, () => {
  it('exists', () => {
    expect(MessageProcessor).toBeTruthy();
  });

  // TODO: add tests https://mygameapp.atlassian.net/browse/KWOKKA-395
});
