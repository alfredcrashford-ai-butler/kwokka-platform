import { ClientRegistry } from './client-registry';

describe(ClientRegistry, () => {
  it('exists', () => {
    expect(ClientRegistry).toBeTruthy();
  });

  // TODO: add tests https://mygameapp.atlassian.net/browse/KWOKKA-395
});
