import { CaptchaMiddleware } from './captcha.middleware';

describe(CaptchaMiddleware, () => {
  it('exists', () => {
    expect(CaptchaMiddleware).toBeTruthy();
  });

  // TODO: add tests https://mygameapp.atlassian.net/browse/KWOKKA-395
});
